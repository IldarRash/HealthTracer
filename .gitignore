node_modules/
dist/
.turbo/
.next/
.expo/
coverage/
*.tsbuildinfo
*.log
*.bak

.env
.env.*
!.env.example
!.env.*.example

# MCP config contains local API keys — never commit the live file; use .cursor/mcp.json.example
.cursor/mcp.json

apps/api/.data/

# transient design-audit scratch (screenshots, local server)
.audit/
